{
  "name": "ai-chat",
  "private": true,
  "packageManager": "pnpm@8.15.9",
  "scripts": {
    "web:dev": "pnpm --dir apps/web dev",
    "web:build": "pnpm --dir apps/web build",
    "relay-cli:check": "pnpm --dir packages/relay-cli check",
    "relay-cli:test": "pnpm --dir packages/relay-cli test"
  }
}
